# ===========================================
# 🚀 支付系统配置示例 - AI Toolbox Pro
# ===========================================

# 🔧 使用说明：
# 1. 复制此文件为 .env.local
# 2. 将下方的示例密钥替换为你的真实 API 密钥
# 3. 测试环境使用 test 密钥，生产环境使用 live 密钥

# ===========================================
# 💳 Creem 支付配置（主要支付方式）
# ===========================================
# 获取方式：登录 Creem 控制台 > 开发者设置 > API 密钥
# 测试环境密钥通常以 ck_test_ 开头
# 生产环境密钥通常以 ck_live_ 开头

CREEM_API_KEY=ck_test_your_api_key_here
CREEM_SECRET_KEY=sk_test_your_secret_key_here
CREEM_WEBHOOK_SECRET=whsec_test_your_webhook_secret_here

# ===========================================
# 💰 PayPal 支付配置（可选）
# ===========================================
# 获取方式：登录 PayPal Developer Dashboard > Applications > Your App
# 测试环境：使用 Sandbox 模式的 Client ID 和 Secret
# 生产环境：使用 Live 模式的 Client ID 和 Secret

PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here
PAYPAL_MODE=sandbox  # sandbox 用于测试，live 用于生产

# ===========================================
# 🎯 Stripe 支付配置（可选）
# ===========================================
# 获取方式：登录 Stripe Dashboard > Developers > API keys
# 测试环境：使用以 sk_test_ 开头的密钥
# 生产环境：使用以 sk_live_ 开头的密钥

STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_MODE=test  # test 用于测试，live 用于生产

# 应用配置
NUXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
NUXT_PUBLIC_SITE_NAME=AI Toolbox Pro
NUXT_PUBLIC_CURRENCY=USD

# 开发环境配置
NODE_ENV=development

# ================================
# Vercel 部署配置
# ================================

# 部署到 Vercel 时，请在 Vercel Dashboard 中设置以下环境变量：

# 生产环境
NODE_ENV=production

# PayPal 支付完整配置
PAYPAL_MODE=sandbox  # 开发: sandbox, 生产: live

# Vercel KV 存储（可选，用于支付记录持久化）
# KV_URL=your_kv_url_here
# KV_REST_API_URL=your_kv_rest_api_url_here
# KV_REST_API_TOKEN=your_kv_rest_api_token_here

# 其他服务（可选）
# SMTP_HOST=your_smtp_host
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password

# ================================
# Webhook URLs 配置
# ================================

# 部署后，请在各支付平台设置以下 Webhook URLs：
# Creem: https://your-domain.vercel.app/api/pay/webhook
# PayPal: https://your-domain.vercel.app/api/pay/webhook
# Stripe: https://your-domain.vercel.app/api/pay/webhook

# ================================
# 部署说明
# ================================

# 1. 将项目部署到 Vercel:
#    vercel --prod

# 2. 在 Vercel Dashboard 中配置环境变量:
#    - Project Settings → Environment Variables
#    - 添加上述所有必需的变量

# 3. 配置支付服务:
#    - 注册各支付平台账号
#    - 获取 API 密钥和 Webhook 配置
#    - 设置正确的 Webhook URLs

# 4. 测试支付功能:
#    - 先在测试环境验证
#    - 确认所有支付流程正常工作
#    - 验证 Webhook 回调处理

# 5. 监控和维护:
#    - 监控 API 性能
#    - 查看错误日志
#    - 定期检查支付状态